# Copy to .env and fill in. DOMAIN and EMAIL are the only required values;
# the rest carries sane defaults.
DOMAIN=node.example.com
EMAIL=admin@example.com
# TAG=de
# TZ=Europe/Moscow LOG_LEVEL=info
